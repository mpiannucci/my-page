CREATE TABLE entries (
    id INT AUTO_INCREMENT,
    title TEXT,
    content TEXT,
    posted_on DATETIME,
    primary key (id),
    likes INT,
    dislikes INT
);

CREATE TABLE users (
	id INT AUTO_INCREMENT,
	username TEXT,
	password TEXT,
	primary key (id)
);